# 🏛️ سامانه مدیریت چک تیسا (Tisa Check System)
### سیستم هوشمند مدیریت اسناد مالی، یادآوری سررسید و ردیابی وقایع

این مستند شامل راهنمای کامل نصب و راه‌اندازی سیستم بر روی توزیع‌های مبتنی بر لینوکس (Ubuntu/Debian/CentOS) است.

---

## 🏗️ معماری سیستم و نحوه اتصال به تلگرام

### ۱. معماری نرم‌افزار (Software Architecture)
سیستم از معماری **Client-Side Centralized** بهره می‌برد:
- **Core Logic:** پیاده‌سازی شده با React 19 و TypeScript برای تضمین پایداری نوع داده‌ها.
- **Persistence Layer:** استفاده از یک لایه انتزاعی دیتابیس (Database Abstraction Layer) بر روی LocalStorage برای دسترسی فوق‌سریع و آفلاین.
- **AI Engine:** اتصال مستقیم به مدل **Gemini 3 Flash** برای تحلیل ریسک و نقدینگی.
- **UI/UX:** طراحی مدرن و RTL با Tailwind CSS و انیمیشن‌های بهینه شده.

### ۲. نحوه اتصال بات تلگرام به هسته اصلی
اتصال بات تلگرام در این سیستم به دو صورت انجام می‌شود:
- **Outbound Notifications (ارسال اعلان):** هسته سیستم با هر تغییر در دیتابیس (ثبت، ویرایش یا حذف)، یک درخواست امن HTTPS به `api.telegram.org` ارسال می‌کند.
- **Automated Reminders (یادآوری خودکار):** در هر بار بارگذاری اپلیکیشن، سرویس `db.checkReminders` تمامی چک‌های در انتظار را بررسی کرده و در صورت نزدیک شدن به زمان تنظیم شده، پیام هشدار را مخابره می‌کند.

---

## 🚀 راهنمای نصب صفر تا صد (Linux Setup)

برای نصب و اجرا، دستورات زیر را به ترتیب در ترمینال خود کپی و اجرا کنید:

### گام اول: بروزرسانی مخازن و نصب Node.js
```bash
# بروزرسانی سیستم
sudo apt update && sudo apt upgrade -y

# نصب ابزارهای مورد نیاز
sudo apt install -y curl git build-essential

# نصب Node.js (نسخه 20 LTS توصیه می‌شود)
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
```

### گام دوم: دریافت پروژه و نصب وابستگی‌ها
```bash
# کلون کردن پروژه (در صورت وجود مخزن)
# git clone https://github.com/your-repo/tisa-check.git
# cd tisa-check

# نصب پکیج‌های پایه
npm install
```

### گام سوم: تنظیم متغیرهای محیطی
یک فایل به نام `.env` در ریشه پروژه بسازید و کلید API خود را قرار دهید:
```bash
echo "API_KEY=YOUR_GEMINI_API_KEY_HERE" > .env
```

### گام چهارم: اجرای آزمایشی (Development)
برای اجرای سیستم در حالت توسعه:
```bash
npm run dev
```

### گام پنجم: استقرار نهایی (Production Deployment)
برای ارايه خروجی نهایی و اجرا بر روی سرور واقعی:
```bash
# ایجاد نسخه بهینه شده
npm run build

# نصب یک وب‌سرور سبک برای سرو کردن فایل‌های استاتیک
sudo npm install -g serve

# اجرای دائمی با PM2 (توصیه شده برای لینوکس)
sudo npm install -g pm2
pm2 start "serve -s dist -p 3000" --name "tisa-check"
```

---

## 🤖 راهنمای فعال‌سازی بات تلگرام

۱. در تلگرام به @BotFather پیام دهید و `/newbot` را بزنید.
۲. نام و یوزرنیم بات را انتخاب کرده و **API Token** را دریافت کنید.
۳. برای پیدا کردن **Chat ID** خود، به بات @userinfobot پیام دهید.
۴. در پنل مدیریت تیسا، به بخش **«درگاه اطلاع‌رسانی»** بروید.
۵. توکن و چت‌آیدی را وارد کرده و دکمه **«ذخیره و همگام‌سازی»** را بزنید.
۶. برای اطمینان، دکمه **«ارسال پیام تست»** را فشار دهید.

---

## 🛡️ نکات امنیتی و نگهداری
- **پشتیبان‌گیری:** از آنجایی که دیتابیس در مرورگر ذخیره می‌شود، برای انتقال داده‌ها بین سیستم‌ها، از ابزار Export (در نسخه‌های آتی) یا کپی کردن مقادیر LocalStorage استفاده کنید.
- **دسترسی:** پیشنهاد می‌شود برای امنیت بیشتر، روی پورت ۳۰۰۰ سرور خود، یک Reverse Proxy با Nginx و گواهی SSL (Let's Encrypt) تنظیم کنید.

**توسعه داده شده توسط: تیم مهندسی تیسا**
**نسخه: ۱.۲.۰ - پایداری کامل**
