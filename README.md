# 🏛️ راهنمای رفع مشکل صفحه سفید و استقرار نهایی

اگر هنوز صفحه سفید می‌بینید، لطفاً این دستورات را دقیقاً به همین ترتیب در ترمینال سرور اجرا کنید:

---

## 🚀 گام‌های طلایی برای پاکسازی و نصب مجدد

```bash
# ۱. ورود به پوشه پروژه
cd yadavar

# ۲. پاکسازی کامل فایل‌های قدیمی و کش بیلد
rm -rf dist
rm -rf node_modules
rm package-lock.json

# ۳. نصب مجدد پکیج‌ها (با اطمینان از سلامت مخازن)
npm install

# ۴. تنظیم کلید API در محیط سیستم (بسیار مهم قبل از بیلد)
# بجای عبارت زیر، کلید واقعی خود را بگذارید
export API_KEY="کلید_شما_اینجا"

# ۵. بیلد کردن پروژه (در این مرحله کلید داخل کدها تزریق می‌شود)
npm run build

# ۶. اجرای مجدد با PM2
pm2 delete tisa-app || true
pm2 start "serve -s dist -p 3000" --name "tisa-app"
```

### 🔍 چرا صفحه سفید می‌شد؟
در نسخه قبلی، مرورگر سعی می‌کرد پکیج‌ها را از اینترنت (esm.sh) بگیرد اما کدهای بیلد شده شما سعی می‌کردند از فایل‌های داخلی استفاده کنند. این تداخل باعث توقف اجرای React می‌شد. در این نسخه، همه چیز به صورت یکپارچه توسط Vite مدیریت می‌شود.

**بررسی وضعیت آنلاین بودن:**
`pm2 status`
