
# 🏛️ راهنمای جامع نصب سامانه تیسا (لینوکس - صفر تا صد)

این راهنما برای توزیع‌های Ubuntu/Debian تهیه شده است. برای نصب بدون خطا، دستورات زیر را به ترتیب در ترمینال کپی کنید.

---

## 🛠 گام ۱: نصب پیش‌نیازهای سیستمی
ابتدا باید Node.js و ابزارهای کامپایل را نصب کنیم.

```bash
# بروزرسانی کلی سیستم
sudo apt update && sudo apt upgrade -y

# نصب ابزارهای ضروری لینوکس
sudo apt install -y curl git build-essential python3

# نصب Node.js نسخه 20 (LTS)
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# بررسی نصب صحیح (باید نسخه را چاپ کند)
node -v
npm -v
```

---

## 📂 گام ۲: آماده‌سازی پروژه
یک پوشه برای پروژه بسازید و فایل‌ها را در آن قرار دهید.

```bash
# ایجاد پوشه پروژه
mkdir tisa-check && cd tisa-check

# در این مرحله باید تمام فایل‌های ارسالی (index.html, App.tsx, package.json و ...) را در این پوشه قرار دهید.
# اگر از قبل فایل‌ها را دارید، از این مرحله عبور کنید.
```

---

## 📦 گام ۳: نصب وابستگی‌های نرم‌افزاری
این دستور تمامی کتابخانه‌های React، Gemini و Recharts را نصب می‌کند.

```bash
# نصب پکیج‌ها (اگر ارور دسترسی داد از sudo استفاده نکنید، دسترسی پوشه را اصلاح کنید)
npm install
```

---

## 🔑 گام ۴: تنظیم API Key
برای بخش هوش مصنوعی (Gemini)، باید کلید خود را در محیط لینوکس تعریف کنید:

```bash
# تعریف کلید در محیط فعلی (بجای کلید زیر، کلید واقعی خود را بگذارید)
export API_KEY="your_actual_gemini_api_key_here"

# برای دائمی شدن در ریبوت‌های بعدی (اختیاری):
echo 'export API_KEY="your_actual_gemini_api_key_here"' >> ~/.bashrc
source ~/.bashrc
```

---

## 🚀 گام ۵: اجرای سامانه
حالا سیستم آماده اجراست.

```bash
# اجرای حالت توسعه (روی پورت 3000)
npm run dev
```
بعد از اجرای دستور بالا، آدرس `http://localhost:3000` را در مرورگر باز کنید.

---

## 🛡 رفع خطاهای احتمالی (Troubleshooting)

**۱. خطای "EACCES" در هنگام نصب:**
این یعنی دسترسی به پوشه محدود است. دستور زیر را بزنید:
`sudo chown -R $USER:$USER .`

**۲. خطای "Command not found: vite":**
یعنی نصب پکیج‌ها ناقص بوده. دستور `npm install` را مجدد اجرا کنید.

**۳. خطای پورت ۳۰۰۰ (Port already in use):**
این دستور را بزنید تا پورت آزاد شود:
`fuser -k 3000/tcp`

---

## 🤖 تنظیم بات تلگرام
۱. توکن را از @BotFather بگیرید.
۲. آیدی عددی را از @userinfobot بگیرید.
۳. در پنل «درگاه اطلاع‌رسانی» وارد کنید.
۴. **نکته:** مطمئن شوید سرور لینوکس شما دسترسی به اینترنت آزاد (بدون فیلتر) برای ارتباط با تلگرام داشته باشد.

---
**تیم پشتیبانی تیسا - نسخه ۱.۲.۰ پایدار**
