# 🏛️ راهنمای استقرار سریع سامانه تیسا (نسخه لینوکس)

این راهنما برای نصب سریع و بدون خطا روی سرورهای **Ubuntu/Debian** آماده شده است. تمامی دستورات را به ترتیب کپی و در ترمینال وارد کنید.

---

## 🚀 مرحله ۱: نصب پیش‌نیازهای سیستمی
ابتدا پکیج‌های مورد نیاز لینوکس و آخرین نسخه پایدار Node.js را نصب می‌کنیم:

```bash
# بروزرسانی مخازن
sudo apt update && sudo apt upgrade -y

# نصب ابزارهای پایه
sudo apt install -y curl git build-essential

# نصب Node.js 20 LTS
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
```

---

## 📂 مرحله ۲: دریافت کدها از گیت‌هاب
کدها را مستقیماً از مخزن شما کلون کرده و وارد پوشه پروژه می‌شویم:

```bash
# کلون کردن مخزن
git clone https://github.com/msorenm/yadavar.git

# ورود به پوشه پروژه
cd yadavar
```

---

## 📦 مرحله ۳: نصب وابستگی‌ها و تنظیم API_KEY
در این مرحله پکیج‌های جاوااسکریپت نصب شده و کلید هوش مصنوعی تعریف می‌شود:

```bash
# نصب تمامی پکیج‌های لیست شده در package.json
npm install

# تنظیم کلید Gemini (بجای عبارت زیر، کلید واقعی خود را قرار دهید)
# این دستور باعث می‌شود در ریبوت‌های بعدی هم کلید فعال بماند
echo 'export API_KEY="کلید_شما_اینجا"' >> ~/.bashrc
source ~/.bashrc
```

---

## 🏛️ مرحله ۴: اجرای دائمی (Production Mode)
برای اینکه با بستن ترمینال سایت قطع نشود، از مدیر پروسه PM2 استفاده می‌کنیم:

```bash
# ۱. بیلد کردن نسخه نهایی
npm run build

# ۲. نصب سرویس‌دهنده و مدیر پروسه
sudo npm install -g serve pm2

# ۳. اجرای سایت روی پورت 3000 به صورت دائمی
pm2 start "serve -s dist -p 3000" --name "tisa-app"

# ۴. تنظیم اجرای خودکار در هنگام روشن شدن سرور
pm2 save
pm2 startup
```

---

## 🛠 عیب‌یابی سریع (Troubleshooting)

**۱. اگر پورت ۳۰۰۰ اشغال بود:**
```bash
sudo fuser -k 3000/tcp
pm2 restart tisa-app
```

**۲. مشاهده وضعیت اجرای برنامه:**
```bash
pm2 status
pm2 logs tisa-app
```

**۳. بروزرسانی کدها به آخرین نسخه گیت‌هاب:**
```bash
git pull origin main
npm install
npm run build
pm2 restart tisa-app
```

---
**نکته امنیتی:** اگر از فایروال لینوکس (UFW) استفاده می‌کنید، پورت ۳۰۰۰ را باز کنید:
`sudo ufw allow 3000/tcp`
