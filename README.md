# 🏛️ راهنمای نصب و استقرار صفر تا صد سامانه تیسا (Linux Implementation)

این راهنما برای نصب بر روی توزیع‌های مبتنی بر **Ubuntu 22.04+** یا **Debian 11+** طراحی شده است.

---

## 🛠 گام ۱: آماده‌سازی زیرساخت لینوکس
ابتدا باید مطمئن شویم که نسخه صحیح Node.js (حداقل ۲۰) روی سیستم شما نصب است.

```bash
# ۱. بروزرسانی مخازن
sudo apt update && sudo apt upgrade -y

# ۲. نصب ابزارهای پیش‌نیاز
sudo apt install -y curl git build-essential python3 psmisc

# ۳. نصب Node.js 20 (LTS) - روش استاندارد NodeSource
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# ۴. تایید نصب (نسخه Node باید v20.x.x باشد)
node -v
```

---

## 📂 گام ۲: ایجاد پروژه و مدیریت فایل‌ها
اگر فایل‌ها را به صورت دستی آپلود می‌کنید، مراحل زیر را طی کنید:

```bash
# ۱. ایجاد پوشه و ورود به آن
mkdir -p ~/tisa-check && cd ~/tisa-check

# ۲. اصلاح دسترسی‌ها (بسیار مهم: برای جلوگیری از ارور EACCES)
sudo chown -R $USER:$USER .
chmod -R 755 .
```

---

## 📦 گام ۳: نصب وابستگی‌های نرم‌افزاری (Critical)
در این مرحله تمامی کتابخانه‌های React و Gemini نصب می‌شوند.

**نکته برای کاربران داخل ایران:** اگر در نصب پکیج‌ها با ارور Timeout مواجه شدید، از آینه (Mirror) استفاده کنید:
`npm config set registry https://registry.npmmirror.com`

```bash
# نصب پکیج‌ها
npm install

# در صورت مواجه شدن با ارورهای Peer Dependency:
# npm install --legacy-peer-deps
```

---

## 🔑 گام ۴: تنظیمات محیطی و API Key
سیستم برای بخش هوش مصنوعی نیاز به کلید Gemini دارد.

```bash
# ۱. ساخت فایل محیطی
nano .env
```
در فایل باز شده، خط زیر را کپی کرده و بجای مقدار نمونه، کلید خود را قرار دهید:
`API_KEY=your_secret_gemini_api_key_here`
*(برای ذخیره: Ctrl+O و سپس Enter و برای خروج: Ctrl+X)*

---

## 🚀 گام ۵: اجرای سامانه (دو روش)

### روش اول: اجرای آزمایشی (Development)
مناسب برای تست اولیه:
```bash
npm run dev -- --host
```
سپس آدرس نمایش داده شده (معمولاً پورت ۳۰۰۰) را در مرورگر باز کنید.

### روش دوم: اجرای دائمی (Production) - توصیه شده
برای اینکه با بستن ترمینال، سایت قطع نشود:
```bash
# ۱. بیلد کردن پروژه
npm run build

# ۲. نصب PM2 برای مدیریت پروسه
sudo npm install -g pm2

# ۳. اجرای دائمی روی پورت 3000
pm2 start "npx serve -s dist -p 3000" --name "tisa-check"

# ۴. تنظیم اجرای خودکار بعد از ریبوت سرور
pm2 save
pm2 startup
```

---

## 🛡 رفع خطاهای متداول لینوکس (Troubleshooting)

**۱. ارور `GLIBC_2.27 not found`:**
نشان‌دهنده قدیمی بودن سیستم عامل شماست. سیستم عامل خود را به Ubuntu 22.04 یا بالاتر ارتقا دهید.

**۲. ارور `Port 3000 already in use`:**
یک برنامه دیگر در حال استفاده از این پورت است. با این دستور آن را آزاد کنید:
```bash
sudo fuser -k 3000/tcp
```

**۳. ارور `Permission Denied` در npm install:**
هرگز `npm install` را با `sudo` اجرا نکنید. اگر به مشکل خوردید، دستور گام ۲ (chown) را مجدد اجرا کنید.

**۴. عدم اتصال به تلگرام:**
مطمئن شوید سرور شما به `api.telegram.org` دسترسی دارد. در سرورهای داخل ایران ممکن است نیاز به تنظیم Proxy یا استفاده از ابزارهایی مثل `shecan` داشته باشید.

---
**پایانی:** حالا سامانه تیسا با قدرت در محیط لینوکس شما در حال اجراست! 🚀
