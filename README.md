# 🏛️ راهنمای استقرار نهایی و رفع خطای صفحه سفید

اگر با صفحه سفید مواجه شدید، به دلیل این است که متغیر `API_KEY` در زمان **Build** (نه زمان اجرا) باید در سیستم وجود داشته باشد.

---

## 🚀 گام‌های اصلاحی (به ترتیب اجرا کنید)

```bash
# ۱. ورود به پوشه پروژه
cd yadavar

# ۲. حذف نسخه قبلی بیلد شده برای اطمینان
rm -rf dist

# ۳. تنظیم کلید API در محیط لینوکس (بسیار مهم)
# بجای عبارت زیر، کلید واقعی خود را بگذارید
export API_KEY="your_actual_gemini_api_key"

# ۴. اجرای مجدد نصب و بیلد (با کلید جدید)
npm install
npm run build

# ۵. ری استارت کردن سرویس PM2
pm2 restart tisa-app || pm2 start "serve -s dist -p 3000" --name "tisa-app"
```

### 💡 نکته مهم:
در فریمورک‌هایی مثل Vite، وقتی دستور `npm run build` را می‌زنید، مقدار `process.env.API_KEY` از محیط سیستم‌عامل خوانده شده و **داخل کدهای جاوااسکریپت حک می‌شود**. پس حتماً قبل از `npm run build` دستور `export API_KEY=...` را اجرا کنید.

---
**مشاهده لاگ‌ها در صورت بروز مشکل:**
`pm2 logs tisa-app`
